# tmrw Help — Root AGENTS.md

- DOX is a high-performance AGENTS.md hierarchy installed here.
- Agents must follow DOX instructions across any edits.

## Core Contract

- AGENTS.md files are binding work contracts for their subtrees.
- Work must stay understandable from the nearest applicable AGENTS.md plus every parent above it.

## Read Before Editing

1. Read this root AGENTS.md.
2. Identify every file/folder you expect to touch.
3. Walk from repo root to each target, reading every AGENTS.md on the route.
4. Use the nearest AGENTS.md as the local contract; parents for repo-wide rules.
5. If docs conflict, the closer doc wins on local detail — but no child may weaken DOX.

Do not rely on memory. Re-read the applicable DOX chain in the current session before editing.

## Update After Editing

Every meaningful change requires a DOX pass before the task is done. Update the closest owning AGENTS.md when a change affects purpose, scope, durable structure, contracts, workflows, inputs/outputs, constraints, user preferences, or the Child DOX Index. Update parents when parent-level structure/ownership/index changes. Delete stale text immediately.

## Purpose

Public help site for the **tmrw education platform**. Docusaurus static site → GitHub Pages at `https://tmrw-education.github.io/help/`. One knowledge base, multiple product surfaces. See `_internal/HELP_STRATEGY.md` for the layered-help vision (tooltips + KB site + Felix AI).

## Stack

- Docusaurus **3.10.1**, classic preset, `@docusaurus/faster` enabled. React 19. TypeScript.
- Config: `docusaurus.config.ts`. Sidebars: `sidebars.ts` (single autogenerated config shared by all sections).
- Build: `npm run build` → `build/`. Dev: `npm start`. Typecheck: `npm run typecheck`.
- Deploy: `deploy.yml` lives in the **content** repo (`tmrw-education/help`), not here — it checks out both repos and publishes to GitHub Pages on push to `help`'s `main`, on a daily cron catch-all, and via manual dispatch. Push a tooling-only change here, then trigger a rebuild from `help` (empty commit or manual dispatch) to publish it.
- `docs`/`blog` presets are **off**. Content is served by seven separate `plugin-content-docs` instances.

## Content Map (plugin id → source dir → route)

| Plugin id | Source dir | Route       | Nav label                |
| --------- | ---------- | ----------- | ------------------------ |
| `sxp`     | `sxp/`     | `/sxp`      | StaffXP                  |
| `ess`     | `ess/`     | `/ess`      | Employee Self-Service    |
| `fo`      | `finops/`  | `/fo`       | Finance & Operations     |
| `pxp`     | `pxp/`     | `/pxp`      | Parent Experience        |
| `lxp`     | `lxp/`     | `/lxp`      | Learner Experience       |
| `hr`      | `hr/`      | `/hr`       | Human Resources          |
| `scm`     | `scm/`     | `/scm`      | Supply Chain Management  |

- Note: source dir name ≠ route only for `finops→fo` now. Keep config and nav in sync.
- `ce/` exists but is **not** wired as a plugin (no route, not in nav). Leave alone unless asked.
- Each content dir holds `.md`/`.mdx` articles; `README.md`, `.pdf`, `.DS_Store`, `99-Images/README.md` are excluded from the build.

## Brand Source of Truth

- Brand system: the **`tmrw-theme` skill** (Carbon-based — IBM Plex Sans/Mono, Mona Sans display, blue `#0f62fe` accent, 8pt grid). Treat the skill as canonical for tokens; some asset **paths in it are stale**.
- Real logos live at `~/Documents/GitHub/tmrw/slide-examples/{light-mode,dark-mode}-logo.svg` (4:1 ratio). The default Docusaurus dino in `static/img/logo.svg` is a placeholder to replace.
- Fonts (Mona Sans, IBM Plex Sans/Mono) are installed in `~/Library/Fonts/` but must be self-hosted or CDN-loaded for the web build.

## Internal Docs (not published)

- `_internal/HELP_STRATEGY.md` — help system strategy/direction.
- `_internal/AUTHORING_FOR_FELIX.md` — content authoring conventions.
- `_internal/RELEASE_NOTES.md` — release log.

## Work Guidance

- Match Docusaurus + Infima conventions. Override brand via CSS custom properties in `src/css/custom.css`, not by forking theme internals unless necessary.
- After config/nav/route changes, run `npm run build` and `npm run typecheck`.

## Verification

- `npm run typecheck` — TS check.
- `npm run build` — must complete without broken-link errors regressing (currently `onBrokenLinks: 'warn'`).

## User Preferences

- Communication: caveman mode (terse) is active this session for chat; code/commits/PRs stay normal prose.

## Child DOX Index

- `src/AGENTS.md` — site theme code: homepage, custom CSS/brand tokens, custom pages. Where the visual redesign happens.
